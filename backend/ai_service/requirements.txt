# FastAPI & Server
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
gunicorn==22.0.0
httpx==0.27.0

# ML / AI
transformers>=4.49.0
chronos-forecasting==2.0
onnxruntime==1.18.0
stable-baselines3==2.3.0
gymnasium==0.29.1
# Learned ranker (LightGBM win-probability model + isotonic calibration).
# Optional at runtime: the serving path degrades to the composite formula when
# these are absent, so a zero-dependency install still runs.
lightgbm==4.5.0
scikit-learn>=1.3.0

# Numerical
numpy==1.26.4
pandas>=2.0.0
ta
yfinance

# CORS & HTTP
python-multipart==0.0.9

# Logging
python-json-logger==2.0.7
psycopg2-binary==2.9.9
