# Final Backtest Verification — post signal-quality apply passes

Date: 2026-07-16 | Commands: `npx tsx backend/scripts/backtest_setups.ts --days 180`
(2058 instruments, 5-bar hold, 0.05%/side costs, honest fills, stop-first tie-break) and
`npx tsx backend/scripts/backtest_filters.ts --days 180` | Baseline: `.backtest_results.txt`

**Note on scope:** two concurrent apply passes landed in this working tree and both are in
the code that was verified here: (a) scanner selectivity changes — scan acceptance threshold
5.8 → 6.5, ADX < 16 penalty, sector-rotation adjustments, session-aligned 4h aggregation,
dynamic minimum R:R floor in `suggestions/generator.ts` — and (b) a hard entry-quality gate
on MOMENTUM_CONTINUATION in `scoreSetupQuality` (`stock_scanner.ts` ~line 1360) enforcing
VOLUME ≥ 1.5× prior-20-bar average, |entry−stop|/ATR14 ≥ 0.8, and favorable 60-bar relative
strength jointly, plus a refreshed evidence comment on `NEGATIVE_EXPECTANCY_SETUPS`
(membership unchanged: BREAKDOWN, BEAR_MOMENTUM, BOLLINGER_SQUEEZE_BREAKOUT,
LIQUIDITY_SWEEP, BREAKOUT). This report supersedes the earlier draft at this path and
covers both.

## 1. Per-setup before/after (raw detector level)

| Setup | Signals (before → after) | WinRate% (before → after) | Expectancy% (before → after) | Delta |
|---|---|---|---|---|
| MOMENTUM_CONTINUATION | 5549 → 5549 | 26.4 → 26.4 | −0.224 → −0.224 | none |
| BEAR_MOMENTUM | 689 → 689 | 8.7 → 8.7 | −0.454 → −0.454 | none |
| LIQUIDITY_SWEEP | 3732 → 3732 | 16.4 → 16.4 | −0.484 → −0.484 | none |
| BREAKOUT | 239 → 239 | 14.3 → 14.3 | −0.813 → −0.813 | none |
| BOLLINGER_SQUEEZE_BREAKOUT | 876 → 876 | 15.0 → 15.0 | −1.463 → −1.463 | none |
| BREAKDOWN | 244 → 244 | 4.3 → 4.3 | −1.606 → −1.606 | none |

**The re-run is identical to the baseline.** This is expected, not an error: both apply
passes changed *gating and scoring* (which signals survive), not the setup detectors in
`technical.ts` that `backtest_setups.ts` exercises directly (only an IST date-shift fix in
weekly aggregation landed there; no detector threshold changed). No regressions, and also
no detector-level improvements — the improvement, if any, lives entirely in selection.
Hold/expiry and R-multiple defaults were not changed by the momentum-gate pass, so the
script's fixed `--holdBars 5` remains the correct apples-to-apples geometry against the
baseline.

## 2. Live-gating verification: `backtest_filters.ts` (current code)

Because the changes act at the gate, the quality-filter study is the closer proxy for live
behavior. Re-run against current code, it reproduces the study exactly. For the one gate
that is now *hard-enforced in code* (MOMENTUM_CONTINUATION):

| config | signals | retention | filled | winRate% | expectancy% |
|---|---|---|---|---|---|
| baseline | 5549 | 100% | 5276 | 26.4 | −0.224 |
| VOLUME alone | 885 | 15.9% | 833 | 24.4 | −0.444 |
| RS alone | 2889 | 52.1% | 2736 | 28.4 | −0.078 |
| STOPATR alone | 4681 | 84.4% | 4444 | 26.0 | −0.178 |
| **VOLUME+RS+STOPATR (now enforced)** | 326 | 5.9% | 306 | 29.2 | **+0.011** |

The enforced thresholds match the study definitions exactly; the one translation is RS
(study: 60-bar return above equal-weight universe mean; live: `rs60 > 1.0` vs Nifty), which
is directionally equivalent but not the identical population — the +0.011% carries that
caveat. Effective live delta for momentum: **−0.224% → +0.011% per trade (+0.235 pp)**, at
the cost of ~94% of signal flow (5,549 → ~326 per 180 days universe-wide, before the
scanner's additional live filters cut further).

Best post-hoc filter stacks for the killed setups (all remain disabled — correctly):

| Setup | Baseline exp% | Best combo | Retention | Best exp% | Verdict |
|---|---|---|---|---|---|
| BEAR_MOMENTUM | −0.454 | VOLUME+RS | 9.4% | +0.154 | 61 fills, 11.1% WR, timeout-driven; worsens with hold time — not enabled |
| BREAKOUT | −0.813 | VOLUME+RS | 24.7% | +0.010 | 55 fills — statistical noise; not enabled |
| LIQUIDITY_SWEEP | −0.484 | VOLUME+RS+STOPATR | 3.1% | −0.054 | still negative |
| BOLLINGER_SQUEEZE_BREAKOUT | −1.463 | VOLUME+RSI+STOPATR | 86% | −1.376 | still negative |
| BREAKDOWN | −1.606 | VOLUME+RS | 20.5% | −1.437 | still negative |

The broader live gate stack (score ≥ 6.5, ADX penalty, sector rotation, dynamic R:R, MTF,
delivery %, liquidity floor) is **not** simulated by any script in this repo — the momentum
gate is the only live gate with a direct backtest measurement behind it.

## 3. Battery status

| Check | Result |
|---|---|
| `npx tsx backend/scripts/backtest_setups.ts --days 180` | ✅ ran clean; identical to baseline (§1) |
| `npx tsx backend/scripts/backtest_filters.ts --days 180` | ✅ ran clean; reproduces `.backtest_filters.md` — enforced momentum combo +0.011% on 306 fills |
| `cd backend && npx tsc --noEmit` | ✅ exit 0, no diagnostics |
| `cd backend && npx vitest run` | ✅ 13 files / 50 tests, all passing (incl. `stock_scanner.test.ts`; no test threshold constants needed updating; DB-auth errors in logs are exercised fallback paths inside passing tests) |

## 4. Honest assessment

At the raw-detector level every setup remains a net loser after costs under honest fills at
a 5-bar hold — win rates of 4–26% and expectancies of −0.22% to −1.61% per trade — and
neither apply pass changed that, because both changed *which signals get through*, not what
the detectors emit. What changed live: the five worst setups stay disabled with fresh
evidence written next to the switch, and the one losing tradeable setup (momentum
continuation) now trades only under the single filter combination the data supports, which
historically moves it from −0.22% to breakeven (+0.01% per trade, 29% win rate) on ~6% of
the original signal flow. That breakeven figure rests on 306 historical fills against a
noisy distribution, uses an RS analog rather than the exact study definition, and the rest
of the live gate stack (score ≥ 6.5, ADX, sector rotation, dynamic R:R, MTF, delivery %)
has no end-to-end backtest. Meanwhile the trend-dependent half of the setup catalog
(PULLBACK, EMA9 setups, MACD, mean-reversion) produces no signals in this 180-day window
(EMA200 fallback degenerates the trend classifier) and therefore has zero evidence either
way; backfilling ≥300 trading days is the prerequisite for measuring it. The defensible
investor-facing story is disciplined, reproducible loss avoidance — every kill and every
gate traces to a number that re-ran identically today — not a demonstrated positive edge.
Whether the surviving trade stream makes money has not been shown and needs either a
gate-aware end-to-end backtest or paper-trading data. These numbers do not support a
performance claim.
